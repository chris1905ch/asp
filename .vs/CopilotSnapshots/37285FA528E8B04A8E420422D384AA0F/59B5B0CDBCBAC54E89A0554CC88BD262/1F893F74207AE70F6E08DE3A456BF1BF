/* Set padding to keep content from hitting the edges */
.body-content {
    margin-top: 15px;
    padding-left: 15px;
    padding-right: 15px;
}

/* Override the default bootstrap behavior where horizontal description lists 
   will truncate terms that are too long to fit in the left column 
*/
.dl-horizontal dt {
    white-space: normal;
}

/* Set width on the form input elements since they're 100% wide by default */
input,
select,
textarea {
    max-width: 280px;
}

body {
    font-family: 'Segoe UI', 'Playfair Display', serif;
    background: #f8f5f0;
}

.card {
    border: none;
    box-shadow: 0 8px 32px 0 rgba(140, 106, 79, 0.15);
}

.luxury-bg {
    background: linear-gradient(120deg, #f8f5f0 0%, #e7d9c4 100%);
}
.luxury-btn {
    background: #8C6A4F;
    color: #fff;
    font-weight: bold;
    border-radius: 10px;
    transition: background 0.3s;
}
.luxury-btn:hover {
    background: #6d4c2c;
}
.luxury-logo {
    width: 70px;
    height: 70px;
    border-radius: 50%;
    object-fit: cover;
    box-shadow: 0 4px 16px 0 rgba(140, 106, 79, 0.15);
}
.luxury-title {
    font-family: 'Playfair Display', serif;
    color: #8C6A4F;
    font-size: 1.4rem;
    font-weight: bold;
}
.luxury-sidebar {
    min-width: 230px;
    max-width: 230px;
    min-height: 100vh;
    background: #fff;
    border-top-right-radius: 24px;
    border-bottom-right-radius: 24px;
    box-shadow: 2px 0 16px 0 rgba(140, 106, 79, 0.08);
    z-index: 1000;
}
.luxury-sidebar .components li a {
    color: #8C6A4F;
    font-weight: 500;
    padding: 14px 28px;
    display: flex;
    align-items: center;
    border-radius: 10px;
    transition: background 0.2s, color 0.2s;
    font-size: 1.08rem;
    margin-bottom: 6px;
}
.luxury-sidebar .components li a .luxury-icon {
    margin-right: 12px;
    font-size: 1.2rem;
}
.luxury-sidebar .components li a:hover {
    background: #e7d9c4;
    color: #6d4c2c;
}
.luxury-content {
    width: 100%;
    min-height: 100vh;
}
.welcome-box {
    background: #fff8f2;
    border-radius: 18px;
    box-shadow: 0 2px 12px 0 rgba(140, 106, 79, 0.07);
    color: #8C6A4F;
}
.luxury-card {
    background: #fff;
    border-radius: 18px;
    box-shadow: 0 2px 12px 0 rgba(140, 106, 79, 0.10);
    padding: 2rem 1.2rem 1.2rem 1.2rem;
    text-align: center;
    margin-bottom: 1.5rem;
    transition: transform 0.2s, box-shadow 0.2s;
    cursor: pointer;
}
.luxury-card:hover {
    transform: translateY(-6px) scale(1.03);
    box-shadow: 0 8px 32px 0 rgba(140, 106, 79, 0.18);
}
.luxury-card-icon {
    font-size: 2.5rem;
    color: #8C6A4F;
    margin-bottom: 0.7rem;
}
.luxury-card-title {
    font-size: 1.2rem;
    font-weight: bold;
    color: #8C6A4F;
    margin-bottom: 0.3rem;
}
.luxury-card-desc {
    font-size: 1rem;
    color: #6d4c2c;
}
@media (max-width: 991px) {
    .luxury-sidebar {
        position: fixed;
        left: -230px;
        top: 0;
        height: 100vh;
        z-index: 999;
        transition: left 0.3s;
    }
    .luxury-sidebar.active {
        left: 0;
    }
    .luxury-content.sidebar-open {
        filter: blur(2px) grayscale(0.2);
        pointer-events: none;
        user-select: none;
    }
}
@media (max-width: 768px) {
    .luxury-sidebar {
        position: fixed;
        left: -230px;
        top: 0;
        height: 100vh;
        z-index: 999;
        transition: left 0.3s;
    }
    .luxury-sidebar.active {
        left: 0;
    }
    .luxury-content {
        padding: 1rem !important;
    }
}
@media (max-width: 576px) {
    .luxury-sidebar {
        min-width: 100vw;
        max-width: 100vw;
        border-radius: 0;
        left: -100vw;
    }
    .luxury-sidebar.active {
        left: 0;
    }
    .luxury-content {
        padding: 0.5rem !important;
    }
    .welcome-box {
        padding: 1.2rem 0.5rem;
    }
}
